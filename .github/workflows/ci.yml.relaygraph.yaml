schemaVersion: 1
id: validation.github_actions_ci
kind: validation
metadata:
  command: "GitHub Actions: dotnet build, dotnet test, relaygraph validate --json"
links:
  - rel: realized-by
    to: id:validation.windows_build
    pathHint: SimpleImageSlideShow.sln
  - rel: realized-by
    to: id:validation.windows_tests
    pathHint: SimpleImageSlideShow.Tests/SimpleImageSlideShow.Tests.csproj
  - rel: documents
    to: id:docs.guide.agents
    pathHint: AGENTS.md
